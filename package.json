{
    "name": "saylani-mass-it-hub",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm install --prefix client && npm install --prefix server",
        "build": "npm run build --prefix client && rm -rf public && mkdir -p public && cp -r client/dist/* public/"
    }
}